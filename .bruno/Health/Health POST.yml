info:
  name: Health POST
  type: http
  seq: 2

http:
  method: POST
  url: "{{API_URL}}/health"
  body:
    type: json
    data: |-
      {
        "stringTest": "test",
        "numberTest": 12
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
