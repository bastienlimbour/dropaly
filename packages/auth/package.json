{
  "name": "@dropaly/auth",
  "type": "module",
  "exports": {
    "./server": "./src/index.server.ts"
  },
  "scripts": {
    "auth:generate": "pnpm dlx @better-auth/cli@latest generate --config src/auth.cli.ts",
    "typecheck": "tsc --noEmit",
    "lint": "oxlint .",
    "lint:fix": "oxlint . --fix",
    "format": "oxfmt .",
    "format:check": "oxfmt . --check",
    "clean": "git clean -xdf .cache .turbo dist node_modules"
  },
  "dependencies": {
    "@better-auth/expo": "catalog:auth",
    "@dropaly/db": "workspace:*",
    "@polar-sh/better-auth": "catalog:auth",
    "@polar-sh/sdk": "catalog:auth",
    "better-auth": "catalog:auth"
  },
  "devDependencies": {
    "@dropaly/config": "workspace:*",
    "typescript": "catalog:"
  }
}
