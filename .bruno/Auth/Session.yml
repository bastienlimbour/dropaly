info:
  name: Session
  type: http
  seq: 1

http:
  method: GET
  url: "{{API_URL}}/auth/get-session"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
