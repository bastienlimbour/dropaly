info:
  name: Signin Email
  type: http
  seq: 2

http:
  method: POST
  url: "{{API_URL}}/auth/sign-in/email"
  body:
    type: json
    data: |-
      {
        "email": "test@test.test",
        "password": "test"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
